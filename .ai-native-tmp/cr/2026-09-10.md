# 代码审查 — 2026-09-10

变更范围：LLM provider 抽象层引入（多协议支持 OpenAI / Anthropic）

审查文件：
- src/be/lib/llm/provider.ts (新增)
- src/be/lib/llm/openai-adapter.ts (新增)
- src/be/lib/llm/anthropic-adapter.ts (新增)
- src/be/lib/text-llm.ts (重构)
- src/be/engine/runners/common/react-core.ts (重构)

## 规范审查

✅ 通过：核心项均符合
- 上层调用（text-llm.ts / react-core.ts）已移除直接 `new OpenAI()`，统一走 provider 层的 `streamCompletion`
- adapter 内部的 `new OpenAI()` / `new Anthropic()` 是抽象层实现本身，符合分层设计
- 跨模块导入使用 `@/*` 别名（react-core 引入 `@/be/lib/llm/provider`）
- 环境变量缺失时 adapter/resolveModel 均抛含字段名的 Error

## 代码审查（业务语义）

**P0 — 必须修复（0 项）**
✅ 无

**P1 — 强烈建议（0 项）**
✅ 无

**P2 — 建议优化（2 项）**
- [P2] src/be/lib/llm/anthropic-adapter.ts — `DEFAULT_MAX_TOKENS = 8192` 硬编码，Anthropic 必填 max_tokens，建议后续可配置化
- [P2] src/be/lib/llm/provider.ts:detectProtocol — 协议识别依赖 URL 关键字（`/messages`、`anthropic`），属启发式判断，特殊网关 URL 可能误判，建议后续补充显式配置项

结论：无 P0/P1 阻塞项，可正常提交。

---

# 代码审查（复审，含实测）— 2026-09-10

触发：用户展示 app 运行输出（对未注册的 `/apm-alarm-stats-dongdong-error-api` 未误认为真实 skill，回答正确），要求审查实现。补充实测与静态复审。

## 代码审查（业务语义）

**P0 — 无**

**P1 — 强烈建议（1 项）**
- [P1] `anthropic-adapter.ts:149` — temperature 直接透传。Anthropic Messages 上限为 **1.0**，OpenAI 允许 0–2.0。skill temperature 为用户 frontmatter 自定义（`skills/loader.ts:45`），若某 skill 配 >1.0，切 Anthropic 网关会 400。建议 clamp 到 `[0,1]`。

**P2 — 建议优化（2 项）**
- [P2] `anthropic-adapter.ts:191` — usage 依赖 `message_start.usage.input_tokens`；网关若不下发则 input 计 0，token 预算偏小（仅影响软约束）。
- [P2] `anthropic-adapter.ts:44` — `contentToText` 丢弃多模态 content 的非 text 块；当前仅纯文本，无影响。

## 实测（真实网关 http://llm-gw.jd.local/anthropic）
- ✅ 纯文本流式：返回「你好」
- ✅ 工具调用：模型正确发起 `get_weather({city:"北京"})`，id/name/args 累积正确
- ✅ 多轮工具回填：assistant tool_use + tool_result 回喂后模型正确消费（"晴，25度"）

结论：原 400 已修复，ReAct 完整链路（含工具往返）在 Anthropic 协议下正常。唯一建议修复项为 P1 temperature clamp。

---

# 代码审查（脚本执行能力）— 2026-09-10

变更范围：给 app 模型新增脚本/文件执行能力（3 个静态工具）

审查文件：
- src/be/engine/tools/runCommand.ts（新增）
- src/be/engine/tools/readFile.ts（新增）
- src/be/engine/tools/writeFile.ts（新增）
- src/be/engine/tools/index.ts（注册 staticTools）
- src/be/session/index.ts（导出 lockedWrite）

## 规范审查

✅ 通过：全部核心项
- 后端无 window/document/localStorage
- write_file 走 lockedWrite（复用 session 的写锁），未直接 fs.writeFile
- 未实例化 OpenAI 客户端
- 3 个新 Tool 均已在 staticTools 注册
- 跨模块导入用 @/* 别名

## 代码审查（业务语义）

**P0 — 无**
**P1 — 无**

**P2 — 建议优化（2 项）**
- [P2] runCommand.ts — 无命令白名单/沙箱/门控，等价任意命令执行(RCE)。按用户明确选择（信任本地自用、不加门控）实现，文件头注释已标注风险点供将来对外服务时收紧。非缺陷。
- [P2] writeFile.ts/readFile.ts — lockedWrite 锁按 filePath 字符串键，相对/绝对路径指向同一文件不命中同锁；当前模型串行调用无并发写风险，无影响。

## 验证
- tsc --noEmit：新增 3 文件 + index/session 改动零报错；仅剩 2 个既有无关错误（McpPanel/SkillsPanel 的 size prop）。
- 端到端（run_command 跑 apm 脚本 → read_file 读报告 → write_file 回填）闭环尚未在运行中的 app 内实测，待用户重载 SkillManager 后验证。

结论：无 P0/P1 阻塞项，可正常提交。

---

# 代码审查（skill 执行接入工具循环）— 2026-09-10

根因：`/command` 触发 skill 时 index.ts:84 走 executeSkill，而 executeSkill 原来只调 chatStream（纯文本、不挂工具）。上一轮新增的 run_command/read_file/write_file 只被 runReActLoop 消费，command-skill 路径不进 ReAct，故模型只能口头描述命令、调不到工具。

变更：src/be/engine/skills/executor.ts —— 从手写 chatStream 循环切到 runReActLoop（内部 getToolDefinitions 已含三个新工具），保留 skill system prompt 组装 + temperature + signal 透传。

## 规范审查
✅ 工具调用循环统一走 runReActLoop，未重复实现；@/* 别名；后端无浏览器 API。

## 代码审查（业务语义）
**P0/P1 — 无**
**P2 — 1 项**
- [P2] 所有 command-skill 现统一走 ReAct（含纯文本 skill）。无工具调用则一轮结束，行为与原 chatStream 等价；每次多一份 tools 定义开销，可忽略。用户已确认「全切 ReAct」。

## 验证
- tsc --noEmit：零新增错误，仅剩 2 个既有无关错误（McpPanel/SkillsPanel size prop）。
结论：无 P0/P1 阻塞项，可正常提交。

---

# 代码审查（日志模块）— 2026-09-10

变更范围：新增统一后端日志模块 + 关键节点插桩

新增：src/be/lib/logger.ts（级别 debug/info/warn/error，SWLWS_LOG_LEVEL 控制，warn/error 走 stderr，仅 console 不落盘）
插桩：engine/index.ts（请求进入/路由决策/异常/中止）、runners/common/react-core.ts（循环迭代/工具调用耗时）、tools/runCommand.ts（命令执行/退出码/超时）、tools/readFile.ts、tools/writeFile.ts、engine/skills/index.ts、tools/mcp/index.ts、lib/llm/provider.ts（协议探测）、memory/mindcards.ts
替换：原有 5 处 console.* 全部改为 logger；src/be/ 已无裸 console.*（logger.ts 除外）
文档：SWLWS_LOG_LEVEL 补进 config.md

## 规范审查
✅ logger.ts 无浏览器 API；不落盘（无 fs.writeFile）；@/* 别名；console.* 清零。

## 代码审查（业务语义）
**P0/P1 — 无**
**P2 — 1 项**
- [P2] logger.ts 级别在模块加载时解析一次，运行时改 SWLWS_LOG_LEVEL 不生效。按用户选择「环境变量、启动期基础设施」设计，符合预期，非缺陷。

## 验证
- tsc --noEmit：零新增错误，仅剩 2 个既有无关错误（McpPanel/SkillsPanel size prop）。
- 级别过滤逻辑冒烟测试：SWLWS_LOG_LEVEL=warn 时 debug/info 被抑制、warn/error 正常输出。

结论：无 P0/P1 阻塞项，可正常提交。

---