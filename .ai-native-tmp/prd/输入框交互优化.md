# 输入框交互优化

当输入“/” 时，能够展示一个浮层，浮层中展示可用的 mcp、skill

视图上 mcp、skill 之间用分割线分开

---

<!-- 评审反馈：以下问题需补充，填写完成后重新运行 /project-prd -->

**[C-1] 需求背景**
请补充：为什么需要这个功能？（背景/痛点/目标）

> 用户体验上，无法感知 skill、mcp

**[C-3] 排除范围**
请补充：明确不做什么？（例如：不支持搜索过滤？不支持键盘导航？不支持嵌套菜单？）

> 支持搜索过滤; 支持键盘导航; 支持嵌套菜单

**[C-5] 主流程步骤**
请补充：完整的用户操作 → 系统响应步骤。（例如：用户在输入框输入 / → 系统展示浮层 → 用户点击某项 → 系统将命令填入输入框 → 浮层关闭）

> 用户在输入框输入 / → 系统展示浮层 → 用户点击某项 → 系统将命令填入输入框 → 浮层关闭

**[C-6] 异常与边界路径**
请补充：以下场景的处理方式：

- mcp/skill 列表均为空时
- 浮层如何关闭（点击外部？按 Esc？选中后自动关闭？）
- 在 / 后继续输入字符时浮层行为

> 展示空内容
> ESC 关闭
> 过滤 mcp、skill

**[C-7] 状态流转**
请补充：浮层从展开到关闭的状态变化。（例如：输入 / → 浮层展开；选中项 → 填充命令 + 浮层关闭；Esc → 浮层关闭；点击外部 → 浮层关闭）

> 输入 / → 浮层展开；选中项 → 填充命令 + 浮层关闭；Esc → 浮层关闭；点击外部 → 浮层关闭

**[C-10] 数据来源**
请补充：mcp 和 skill 列表从哪里获取？（复用现有接口 /api/mcp 和 /api/skills？还是新增接口？）

> 复用现有接口

**[C-14] 正向验收 case**
请补充：至少 1 条正向验收场景。（例如：输入框内容为空时输入 /，浮层出现并列出所有已启用的 mcp server 和 skill，mcp 与 skill 之间有分割线）

> 输入框内容为空时输入 /，浮层出现并列出所有已启用的 mcp server 和 skill，mcp 与 skill 之间有分割线

**[C-15] 异常验收 case**
请补充：至少 1 条异常验收场景。（例如：mcp 和 skill 均为空时，输入 / 不展示浮层）

> mcp 和 skill 均为空时，输入 / 不展示浮层
